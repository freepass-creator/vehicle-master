# Project 3 — Local Codex Final Order

Date: 2026-09-20  
Repository: `freepass-creator/vehicle-master`  
Required branch: `codex/project3-identity-provenance`  
PR: #1 (keep Draft until this order passes)

## Mission

Produce and verify the **first canonical v2 identity registry baseline** from the
real 12,301-node `data/vehicle-tree.json`.

Do not redesign the contract during this run. The semantic UID design has
already been audited against the complete source data.

## Expected ground truth

Source audit expectations:

- total entities: 12,301
- manufacturers: 75
- models: 1,121
- sub_models: 1,805
- powertrains: 2,950
- trims: 6,350
- compatibility-id unique count: 11,123
- compatibility-id duplicate occurrences: 1,178
- compatibility-id distinct collision count: 922
- legacy generations lookup overwrite risk: 110
- semantic identity collision occurrences: **0**

If these materially differ, STOP and investigate the source revision before
creating a baseline.

## Preflight

```bash
git branch --show-current
git status --short
python -m unittest discover -s tests -v
python scripts/audit_identity_source.py
```

Requirements:

- branch is exactly `codex/project3-identity-provenance`
- tests pass
- source audit exits 0
- semantic collision count is 0

## Baseline command

Run exactly once for the first v2 baseline:

```bash
python scripts/bootstrap_identity_baseline.py 2026-09-20
```

The command must:

1. run full-source audit
2. build to staging
3. validate staging
4. rollback-safely promote to `dist/`
5. validate final release
6. cross-check source/export counts
7. cross-check compatibility-id collision metrics
8. verify identity entity count
9. print SHA-256 receipt values

## Required final artifacts

Confirm these exist and are generated by the same version:

- `dist/manifest.json`
- `dist/vehicle-master.json`
- `dist/vehicle-master.flat.json`
- `dist/vehicle-master.flat.csv`
- `dist/codes.json`
- `dist/match-index.json`
- `dist/identity-map.json`
- `dist/provenance.json`
- `dist/SCHEMA.md`

Run again:

```bash
python scripts/validate_identity_export.py
```

It must return `"ok": true`.

## Baseline review

Inspect `dist/identity-map.json`:

- schema_version = `2.0`
- entity_count = `12301`
- no duplicate uid
- every active entity has identity_key
- UID suffix is 32 lowercase hex characters

Inspect `dist/codes.json`:

- `generations_by_uid` contains all 1,805 sub-models
- `generation_id_index` preserves duplicate compatibility ids as uid arrays

## Commit / PR rule

Only after every gate above passes:

1. commit the generated v2 baseline artifacts
2. paste the bootstrap SHA-256 receipt into PR #1
3. keep the receipt with the commit SHA in the PR discussion
4. then mark PR #1 ready for review

Do **not** add GitHub Actions.

Do **not** migrate downstream projects in this commit. Downstream migration from
`id` to `uid` is a separate follow-up after the baseline is merged.

## Failure rule

If bootstrap or validation fails:

- do not manually copy partial staging files into `dist/`
- do not delete an existing approved identity-map
- do not invent aliases to silence a collision
- report the exact failing identity_key / uid / compatibility id and stop the
  baseline promotion until the cause is understood
